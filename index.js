

function addingEventListener(){
    const input = document.getElementById('button');

function clickAlert(_clickAlert) {
  alert('I was clicked!');
}

input.addEventListener('click', clickAlert);

}